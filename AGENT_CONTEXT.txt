RuView Agent Context
====================

Purpose
-------
This file is the persistent memory for Codex work on this project. At the start
of future sessions, read this file before making changes. Whenever important
project information changes, update this file in the same commit or work step.

Update Rules
------------
- Add notes for important setup steps, code/config changes, removals, tests,
  failures, blockers, and decisions.
- Include the reason for each meaningful change, not just the file name.
- Keep entries concise and useful for a future Codex session after context
  compaction.
- Preserve older entries unless they are clearly wrong; append corrections
  instead of erasing history.

Repository Setup
----------------
- Local path: C:\Users\ryuga\OneDrive\Documents\Projetos\RuView
- Fork/origin: https://github.com/IlDottore2nd/RuView.git
- Upstream: https://github.com/ruvnet/RuView.git
- Default branch: main
- Fork was created from ruvnet/RuView via the GitHub web UI.
- Local checkout was cloned from the fork, then the upstream remote was added.

Change Log
----------
2026-07-06
- Created this AGENT_CONTEXT.txt file so future Codex sessions can recover
  project state after token/context compaction.
- Confirmed target project path was empty before cloning.
- Cloned IlDottore2nd/RuView into the target project path.
- Added upstream remote pointing to ruvnet/RuView for future syncs.
- Set repo-local Git author identity:
  IlDottore2nd <143614564+IlDottore2nd@users.noreply.github.com>.
- Committed and pushed the initial context file to origin/main.
- Prepared Docker setup:
  - Initialized and pinned all Git submodules with
    git submodule update --init --recursive --checkout --force.
  - Added docker/docker-compose.local.yml for local simulated Docker runs.
    It binds host ports to 127.0.0.1, sets CSI_SOURCE=simulated, and keeps
    the legacy Python sensing service behind the legacy-python profile.
  - Added scripts/start-docker-demo.ps1 as the Windows-friendly launcher for
    the local compose files. Use -Build for a local image build and -Detached
    to leave containers running in the background.
  - Docker CLI and winget were not available on PATH on this machine, so Docker
    Desktop still needs to be installed or added to PATH before running the demo.
- Repaired Docker Desktop startup blocker:
  - Docker Desktop showed "WSL needs updating"; wsl --update failed with
    Wsl/CallMsi/Install/REGDB_E_CLASSNOTREG.
  - Downloaded and installed Microsoft WSL 2.7.10 x64 MSI from the official
    microsoft/WSL GitHub release.
  - Restarted WSL and Docker Desktop; docker info now reports engine 29.6.1.
  - First RuView compose start failed because host port 3000 was unavailable.
    Changed the local Docker demo host ports to 3300/3301 while keeping
    container ports 3000/3001.
  - Host UDP port 5005 was also unavailable during simulated Docker startup.
    Changed the local host UDP mapping to 5505:5005/udp; ESP32 hardware runs
    can revisit this when real CSI ingestion is needed.
  - Added Compose !override tags so local ports replace the base compose port
    list instead of merging with it.
- Docker/WSL test cleanup after user uninstalled Docker:
  - Uninstalled the Microsoft WSL 2.7.10 package that had been installed to
    repair Docker Desktop startup.
  - Removed the downloaded WSL MSI from the Codex work directory.
  - Removed leftover Docker user config folders:
    C:\Users\ryuga\AppData\Local\Docker,
    C:\Users\ryuga\AppData\Roaming\Docker, and C:\Users\ryuga\.docker.
  - Verified Docker CLI is no longer on PATH and WSL reports "not installed".
  - Kept repo source additions (AGENT_CONTEXT.txt, local compose override, and
    PowerShell demo launcher) as requested.

Next Session Checklist
----------------------
1. Read this file first.
2. Run git status --short --branch.
3. Check git remote -v if repository sync or push behavior matters.
4. After meaningful work, update this file before committing or reporting done.
